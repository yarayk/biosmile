import 'package:flutter/material.dart';

class Additional2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Упражнение 2")),
      body: Center(
        child: Text("Страница Additional2", style: TextStyle(fontSize: 24)),
      ),
    );
  }
}