import 'package:flutter/material.dart';

class Lips3 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Упражнение 3")),
      body: Center(
        child: Text("Страница Lips3", style: TextStyle(fontSize: 24)),
      ),
    );
  }
}