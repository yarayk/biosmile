import 'package:flutter/material.dart';

class Face2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Упражнение 2")),
      body: Center(
        child: Text("Страница Face2", style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
