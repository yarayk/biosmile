import 'package:flutter/material.dart';

class Cheeks3 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Упражнение 3")),
      body: Center(
        child: Text("Страница Cheeks3", style: TextStyle(fontSize: 24)),
      ),
    );
  }
}